# v0.01.0029 — 2026-07-10

执行者: Codex

## 修改

- 9 键输入完整数字串命中多音节词条时，候选栏优先显示整词候选，内置词库与自学习词库都能直接参与召回。
- 左侧拼音栏仍只提供当前第一个音节的拼音选择；用户点选后，候选栏切换为该音节的单字候选，并按原有流程继续处理剩余数字。
- 没有整词命中时，保持逐音节单字组词，不阻断 `fen -> pan` 这类自造词输入路径。

## 验证

- 尚未运行真机/人工测试。
- 下一步应覆盖整词直出、点击拼音后逐音节输入、无整词命中时逐音节组词三类场景。

# v0.01.0030 — 2026-07-10

执行者: Codex

## 修改

- 修复 9 键整词命中与首音节提示不一致的问题：完整数字命中 `qiguai` 等多音节拼音时，左侧优先显示能正确切分该整词的首音节 `qi`，再显示 `shi` 等纯数字歧义项。
- 保留其它合法的数字歧义拼音，避免修复首选项时丢失可选发音。
- 删除候选栏左右翻页按钮，保留紧凑候选行与展开按钮；完整候选统一通过展开面板查看和选择。
- 更新显示版本号为 `v0.01.0030`。

## 验证

- 本机 `assembleDebug` 通过。
- 尚未运行真机/人工测试。
- 手测重点：`744824 -> qi/奇怪`、`744 -> shi` 回归、候选展开面板浏览与选择、`64426 -> 你好` 回归。

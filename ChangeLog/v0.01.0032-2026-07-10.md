# v0.01.0032 — 2026-07-10

执行者: Codex

## 修复

- 修复大词库加载期间整词候选和单音节拼音列表不同步的问题。候选词、数字索引、前缀索引和单音节索引改为全部构建完成后一次性切换。
- 加载期间不再出现“整词已经来自大词库，但左侧拼音仍只有备用词库内容”的半完成状态。
- 从完整基础词库自动生成包含 405 个合法单音节的小型基础词库，并在键盘显示前加载；冷启动拼音选择不再依赖少量手工硬编码条目。
- 新增 `scripts/build_syllable_dict.py`，以后扩充基础词库时可同步重新生成单音节基础词库，对所有词统一生效；`744824` 只作为回归用例，不包含专门判断。
- 更新显示版本号为 `v0.01.0032`。

## 验证

- 本机 `assembleDebug` 通过。
- 尚未运行真机/人工测试。
- 重点测试强制停止后立即输入 `744824`，观察大词库加载前后拼音列表与整词候选是否始终属于同一套词库状态。
- 同时回归 v0.01.0031 的快速输入按键位置稳定性。
